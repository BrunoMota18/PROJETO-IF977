<%= form_with(model: utensilio, local: true) do |form| %>
  <% if utensilio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(utensilio.errors.count, "error") %> prohibited this utensilio from being saved:</h2>

      <ul>
      <% utensilio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <%= hidden_field_tag 'paciente_id', params[:paciente_id] %>  

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_area :descricao %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
